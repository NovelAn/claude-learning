# Python 工具函数库

> 一个简洁实用的 Python 工具函数库，提供常用的数学计算和字符串处理功能。

**版本**: 0.2.0
**作者**: Claude Code 学习者
**学习项目**: Claude Code 基础实战案例

---

## 📦 项目简介

这是一个在 Claude Code 辅导下完成的 Python 学习项目，旨在通过实践掌握 Claude Code 的核心功能，包括：
- 文件引用语法（`@file`, `@directory`）
- Git 提交流程
- 代码分析与改进建议

本项目包含两个核心模块：
1. **数学工具** (`math_utils.py`) - 数值计算相关功能
2. **字符串工具** (`string_utils.py`) - 文本处理相关功能

---

## 🚀 快速开始

### 安装

将本项目克隆到本地：

```bash
git clone <your-repo-url>
cd claude-pratice
```

### 使用示例

#### 方式一：直接导入模块

```python
from math_utils import average, sum_numbers
from string_utils import reverse_string, capitalize_words

# 数学计算
result = average([10, 20, 30, 40, 50])
print(f"平均值: {result}")  # 输出: 平均值: 30.0

# 字符串处理
text = reverse_string("hello world")
print(f"反转后: {text}")  # 输出: 反转后: dlrow olleh
```

#### 方式二：从包导入

```python
from claude_pratice import average, reverse_string

# 使用函数
avg = average([1, 2, 3, 4, 5])
reversed = reverse_string("Claude Code")
```

---

## 📚 功能模块

### 数学工具 (math_utils.py)

#### 基础统计函数

| 函数 | 功能 | 示例 |
|------|------|------|
| `sum_numbers(numbers)` | 计算数字列表的总和 | `sum_numbers([1,2,3])` → `6` |
| `average(numbers)` | 计算数字列表的平均值 | `average([1,2,3,4,5])` → `3.0` |
| `find_max(numbers)` | 找出列表中的最大值 | `find_max([1,5,3])` → `5` |
| `find_min(numbers)` | 找出列表中的最小值 | `find_min([1,5,3])` → `1` |

#### 高级统计函数 (新增)

| 函数 | 功能 | 示例 |
|------|------|------|
| `median(numbers)` | 计算中位数（中间值） | `median([1,3,5])` → `3` |
| `variance(numbers)` | 计算方差（数据离散程度） | `variance([1,2,3,4,5])` → `2.0` |
| `standard_deviation(numbers)` | 计算标准差（方差的平方根） | `standard_deviation([1,2,3,4,5])` → `1.414` |

**特性**:
- ✅ 空列表保护（返回 0 而非抛出异常）
- ✅ 支持所有可迭代数字类型
- ✅ 清晰的错误处理
- ✅ 手动实现，帮助理解算法原理
- ✅ 完整的类型提示（Type Hints）

### 字符串工具 (string_utils.py)

| 函数 | 功能 | 示例 |
|------|------|------|
| `reverse_string(text)` | 反转字符串 | `reverse_string("abc")` → `"cba"` |
| `capitalize_words(text)` | 每个单词首字母大写 | `capitalize_words("hello world")` → `"Hello World"` |
| `count_words(text)` | 统计单词数量 | `count_words("hello world")` → `2` |
| `remove_extra_spaces(text)` | 移除多余空格 | `remove_extra_spaces("a  b")` → `"a b"` |

**特性**:
- ✅ 高效的字符串切片
- ✅ 智能空格处理
- ✅ 准确的单词计数

---

## 🧪 运行测试

### 测试数学工具

```bash
python math_utils.py
```

**预期输出**:
```
测试数据: [10, 20, 30, 40, 50]
总和: 150
平均值: 30.0
最大值: 50
最小值: 10
```

### 测试字符串工具

```bash
python string_utils.py
```

**预期输出**:
```
原字符串: hello world
反转后: dlrow olleh
首字母大写: Hello World
单词数量: 2

原字符串（多余空格）: '  hello    world   python  '
处理后: 'hello world python'
```

---

## 📖 学习历程

本项目是学习 **Claude Code CLI 工具**的实践成果：

### 学到的技能
- ✅ 使用 `@file` 引用单个文件进行分析
- ✅ 使用 `@directory` 引用整个目录进行项目分析
- ✅ 理解 Git 提交流程（手动 vs `/commit` 命令）
- ✅ 根据建议改进代码结构

### 提交历史

```bash
git log --oneline
```

1. `0730b31` - feat: add Python math utility functions
2. `270d7fa` - feat: add string utility functions

---

## 🔧 项目结构

```
claude-pratice/
├── __init__.py           # 包初始化文件
├── math_utils.py         # 数学工具模块
├── string_utils.py       # 字符串工具模块
├── README.md             # 项目文档（本文件）
└── test_utils.py         # 单元测试（待添加）
```

---

## 🎯 未来改进方向

- [ ] 添加完整的单元测试（`test_utils.py`）
- [ ] 为函数添加类型提示（Type Hints）
- [ ] 支持更多数学运算（标准差、中位数等）
- [ ] 添加更多字符串处理功能
- [ ] 集成 CI/CD 自动化测试

---

## 🤝 贡献指南

欢迎提交 Issue 和 Pull Request！

1. Fork 本项目
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'feat: add some amazing feature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

---

## 📝 许可证

本项目采用 MIT 许可证。

---

## 📚 相关资源

- [Claude Code 官方文档](https://docs.anthropic.com/claude-code)
- [Claude Code GitHub](https://github.com/anthropics/claude-code)
- [Python 官方文档](https://docs.python.org/zh-cn/3/)

---

## 🙏 致谢

感谢 Claude Code 提供的强大辅助，让学习变得更加高效！

**最后更新**: 2025-12-30
