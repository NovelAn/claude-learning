# Python 工具函数库

> 一个简洁实用的 Python 工具函数库，提供常用的数学计算和字符串处理功能。

**版本**: 1.0.0
**作者**: Claude Code 学习者
**学习项目**: Claude Code Plan Mode 实战案例

---

## 📦 项目简介

这是一个在 Claude Code 辅导下完成的 Python 学习项目，展示了 **Plan Mode（计划模式）**的实际应用。

本项目包含三个核心模块：
1. **数学工具** (`math_utils.py`) - 数值计算相关功能
2. **字符串工具** (`string_utils.py`) - 文本处理相关功能
3. **装饰器工具** (`decorators.py`) - 公共验证逻辑

**v1.0.0 重大更新**：
- ✅ 添加装饰器系统，减少代码重复
- ✅ 完善 None 和空值错误处理
- ✅ 添加完整的类型提示（Optional 类型）
- ✅ 优化代码结构和可维护性

---

## 🚀 快速开始

### 安装

将本项目克隆到本地：

```bash
git clone <your-repo-url>
cd claude-pratice
```

### 使用示例

#### 方式一：直接导入模块

```python
from math_utils import average, sum_numbers
from string_utils import reverse_string, capitalize_words

# 数学计算（支持 None 输入）
result = average([10, 20, 30, 40, 50])
print(f"平均值: {result}")  # 输出: 平均值: 30.0

# 字符串处理（支持 None 输入）
text = reverse_string("hello world")
print(f"反转后: {text}")  # 输出: 反转后: dlrow olleh
```

#### 方式二：从包导入

```python
from claude_pratice import average, reverse_string

# 使用函数
avg = average([1, 2, 3, 4, 5])
reversed = reverse_string("Claude Code")
```

#### 方式三：使用装饰器

```python
from decorators import validate_non_empty

@validate_non_empty(return_value=0)
def custom_sum(numbers):
    return sum(numbers)

# 现在函数自动处理空列表和 None
print(custom_sum([1, 2, 3]))  # 输出: 6
print(custom_sum([]))         # 输出: 0
print(custom_sum(None))       # 输出: 0
```

---

## 📚 功能模块

### 数学工具 (math_utils.py)

#### 基础统计函数

| 函数 | 功能 | 示例 |
|------|------|------|
| `sum_numbers(numbers)` | 计算数字列表的总和 | `sum_numbers([1,2,3])` → `6` |
| `average(numbers)` | 计算数字列表的平均值 | `average([1,2,3,4,5])` → `3.0` |
| `find_max(numbers)` | 找出列表中的最大值 | `find_max([1,5,3])` → `5` |
| `find_min(numbers)` | 找出列表中的最小值 | `find_min([1,5,3])` → `1` |

#### 高级统计函数

| 函数 | 功能 | 示例 |
|------|------|------|
| `median(numbers)` | 计算中位数（中间值） | `median([1,3,5])` → `3` |
| `variance(numbers)` | 计算方差（数据离散程度） | `variance([1,2,3,4,5])` → `2.0` |
| `standard_deviation(numbers)` | 计算标准差 | `standard_deviation([1,2,3,4,5])` → `1.414` |

**特性**:
- ✅ 空列表和 None 保护（返回 0 而非抛出异常）
- ✅ 使用装饰器减少代码重复
- ✅ 完整的类型提示（包括 Optional 类型）
- ✅ 清晰的错误处理

### 字符串工具 (string_utils.py)

| 函数 | 功能 | 示例 |
|------|------|------|
| `reverse_string(text)` | 反转字符串 | `reverse_string("abc")` → `"cba"` |
| `capitalize_words(text)` | 每个单词首字母大写 | `capitalize_words("hello world")` → `"Hello World"` |
| `count_words(text)` | 统计单词数量 | `count_words("hello world")` → `2` |
| `remove_extra_spaces(text)` | 移除多余空格 | `remove_extra_spaces("a  b")` → `"a b"` |

**特性**:
- ✅ 高效的字符串切片
- ✅ 智能空格处理
- ✅ None 和空字符串保护

### 装饰器工具 (decorators.py) [新增]

| 装饰器 | 功能 | 使用场景 |
|--------|------|----------|
| `@validate_non_empty` | 验证列表/数字不为空 | 数学计算函数 |
| `@validate_string_not_empty` | 验证字符串不为空 | 字符串处理函数 |

**示例**:
```python
from decorators import validate_non_empty

@validate_non_empty(return_value=0)
def my_function(data):
    return sum(data)
```

---

## 🧪 运行测试

### 测试数学工具

```bash
python math_utils.py
```

**预期输出**:
```
测试数据: [10, 20, 30, 40, 50]
总和: 150
平均值: 30.0
最大值: 50
最小值: 10

--- 高级统计功能 ---
中位数: 30.0
方差: 200.0
标准差: 14.142135623730951

测试数据（偶数个）: [10, 20, 30, 40]
中位数: 25.0
```

### 测试字符串工具

```bash
python string_utils.py
```

**预期输出**:
```
原字符串: hello world
反转后: dlrow olleh
首字母大写: Hello World
单词数量: 2

原字符串（多余空格）: '  hello    world   python  '
处理后: 'hello world python'
```

---

## 📖 学习历程：Plan Mode 实战

### 本次优化（v1.0.0）使用的技术

#### 1. Plan Mode 工作流程

本项目优化完全遵循 **Claude Code Plan Mode** 的最佳实践：

```
1. 任务识别 → "优化 Python 工具库"
2. 探索代码库 → 读取 math_utils.py 和 string_utils.py
3. 问题分析 → 识别代码重复和缺少错误处理
4. 方案设计 → 提出 A/B/C 三种优化方案
5. 用户决策 → 选择方案 B（中等优化）
6. 实施改进 → 创建装饰器、优化函数、完善文档
7. 测试验证 → 运行测试确保功能正常
```

#### 2. 优化成果

**代码改进**:
- 减少了 ~40 行重复代码
- 添加了 3 个装饰器
- 完善了所有函数的错误处理
- 提升了代码可维护性

**学到的技能**:
- ✅ 理解 Plan Mode 的使用场景
- ✅ 掌握装饰器的创建和使用
- ✅ 学会减少代码重复的最佳实践
- ✅ 完善类型提示和错误处理

---

## 🔧 项目结构

```
claude-pratice/
├── __init__.py           # 包初始化文件，导出所有函数
├── math_utils.py         # 数学工具模块（使用装饰器）
├── string_utils.py       # 字符串工具模块（使用装饰器）
├── decorators.py         # 装饰器模块（新增）
├── README.md             # 项目文档（本文件）
└── test_utils.py         # 单元测试（待添加）
```

---

## 🎯 版本历史

### v1.0.0 (2025-12-31) - Plan Mode 优化版
- ✅ 添加装饰器系统
- ✅ 完善 None 和空值错误处理
- ✅ 优化代码结构
- ✅ 更新文档

### v0.2.0 (2025-12-30) - 初始版本
- ✅ 基础数学工具（11 个函数）
- ✅ 字符串工具（4 个函数）
- ✅ Git 提交实践

---

## 🤝 贡献指南

欢迎提交 Issue 和 Pull Request！

1. Fork 本项目
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'feat: add some amazing feature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

---

## 📝 许可证

本项目采用 MIT 许可证。

---

## 📚 相关资源

- [Claude Code 官方文档](https://docs.anthropic.com/claude-code)
- [Claude Code GitHub](https://github.com/anthropics/claude-code)
- [Python 官方文档](https://docs.python.org/zh-cn/3/)

---

## 🙏 致谢

感谢 Claude Code 的 Plan Mode 让代码优化变得系统化和高效！

**最后更新**: 2025-12-31
